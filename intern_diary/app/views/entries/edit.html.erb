<h1>Editing entry</h1>

<%= render "form", entry: @entry, form_url: entry_path(params[:username], @entry.id) %>

<br>

<div>
  <%= link_to "Show this entry", entry_path(@entry) %> |
  <%= link_to "Back to entries", entries_path %>
</div>
