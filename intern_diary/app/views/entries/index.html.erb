<p style="color: green"><%= notice %></p>

<h1>Entries</h1>

<div id="entries">
  <% @entries.each do |entry| %>
    <%= render entry %>
    <p>
      <%= link_to "Show this entry", entry_path(id: entry.id) %>
    </p>
  <% end %>
</div>

<%= link_to "New entry", new_entry_path %>
